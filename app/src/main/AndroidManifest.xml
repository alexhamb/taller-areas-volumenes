<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alex6.listview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Areas" />
        <activity android:name=".Realizadas" />
        <activity android:name=".Volumenes" />
        <activity android:name=".areas_Circulo" />
        <activity android:name=".areas_Cuadrado" />
        <activity android:name=".areas_Rectangulo" />
        <activity android:name=".areas_triangulo" />
        <activity android:name=".volumenes_Cilindro" />
        <activity android:name=".volumenes_Cono" />
        <activity android:name=".volumenes_Cubo" />
        <activity android:name=".volumenes_Esfera"></activity>
    </application>

</manifest>